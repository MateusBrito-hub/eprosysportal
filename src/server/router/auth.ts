import { Router } from 'express';
import { login,  } from '../controller';
import { } from '../controller'

const router = Router();

router.post('/login', 
	login
);

router.post('',

)

export {router};